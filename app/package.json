{
  "name": "grafana-prom-node-demo",
  "version": "1.0.0",
  "description": "Example Node.js endpoint that proxies a Grafana-prometheus query alongside Prometheus and Grafana via docker compose.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "hono": "^4.2.7",
    "@hono/node-server": "^1.8.1"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.12.7"
  }
}
